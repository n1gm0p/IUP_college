import type { ScheduleMap } from "@/lib/types";

/**
 * Empty seed — mirrors a Firestore `schedules` collection.
 * Replace with getDocs later.
 */
export function createMockSchedule(): ScheduleMap {
  return {};
}
